<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.9</version>
        <relativePath/>
    </parent>

    <groupId>io.kbrag</groupId>
    <artifactId>kb-rag-server</artifactId>
    <version>1.1.0</version>
    <packaging>pom</packaging>
    <name>kb-rag-server</name>
    <description>Enterprise RAG knowledge base - Java main service</description>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
        </license>
    </licenses>

    <modules>
        <module>kb-common</module>
        <module>kb-domain</module>
        <module>kb-infrastructure</module>
        <module>kb-app</module>
        <module>kb-api</module>
    </modules>

    <properties>
        <java.version>17</java.version>
        <maven.compiler.source>17</maven.compiler.source>
        <maven.compiler.target>17</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <mybatis-plus.version>3.5.7</mybatis-plus.version>
        <elasticsearch-java.version>8.13.4</elasticsearch-java.version>
        <minio.version>8.5.17</minio.version>
        <neo4j-java-driver.version>5.28.9</neo4j-java-driver.version>
        <commons-collections4.version>4.4</commons-collections4.version>
        <!-- Spring Boot 3.3.9 manages Angus Mail 2.0.3. Version 2.0.4 contains the upstream
             CVE-2025-7962 SMTP injection fix and remains API-compatible with this Boot line. -->
        <angus-mail.version>2.0.4</angus-mail.version>
        <!-- Headless Chromium fetcher for M17 JS rendering. Not managed by the Boot BOM, so the
             coordinate is pinned here to keep the browser protocol and the driver jar in step. -->
        <playwright.version>1.47.0</playwright.version>
        <!-- The exact release Spring Security 6.3 (the Boot 3.3 line) builds against, so the jar
             stays in step with the rest of the ecosystem even though the Boot BOM itself does not
             manage this coordinate. -->
        <nimbus-jose-jwt.version>9.37.3</nimbus-jose-jwt.version>
        <!-- 控制台会话与登录态的底座（Apache-2.0）。Boot BOM 不管理该坐标，版本在此显式钉住，
             使会话框架与其 Redis 适配包始终同版本发布，避免二者错位导致的序列化不兼容。 -->
        <sa-token.version>1.46.0</sa-token.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.kbrag</groupId>
                <artifactId>kb-common</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.kbrag</groupId>
                <artifactId>kb-domain</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.kbrag</groupId>
                <artifactId>kb-infrastructure</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.kbrag</groupId>
                <artifactId>kb-app</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>co.elastic.clients</groupId>
                <artifactId>elasticsearch-java</artifactId>
                <version>${elasticsearch-java.version}</version>
            </dependency>
            <dependency>
                <groupId>io.minio</groupId>
                <artifactId>minio</artifactId>
                <version>${minio.version}</version>
            </dependency>
            <dependency>
                <groupId>org.neo4j.driver</groupId>
                <artifactId>neo4j-java-driver</artifactId>
                <version>${neo4j-java-driver.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-collections4</artifactId>
                <version>${commons-collections4.version}</version>
            </dependency>
            <dependency>
                <groupId>com.nimbusds</groupId>
                <artifactId>nimbus-jose-jwt</artifactId>
                <version>${nimbus-jose-jwt.version}</version>
            </dependency>
            <dependency>
                <groupId>com.microsoft.playwright</groupId>
                <artifactId>playwright</artifactId>
                <version>${playwright.version}</version>
            </dependency>
            <dependency>
                <groupId>cn.dev33</groupId>
                <artifactId>sa-token-core</artifactId>
                <version>${sa-token.version}</version>
            </dependency>
            <dependency>
                <groupId>cn.dev33</groupId>
                <artifactId>sa-token-spring-boot3-starter</artifactId>
                <version>${sa-token.version}</version>
            </dependency>
            <!--
              仅在 kb.cache.provider=redis 时装配，见 SaTokenConfig。
              直接取 sa-token-redis-template 而不是 sa-token-redis-jackson：后者是个不含任何类的聚合包，
              只把 sa-token-jackson 和本包拼在一起，而 sa-token-jackson 已由 starter 传递引入。
            -->
            <dependency>
                <groupId>cn.dev33</groupId>
                <artifactId>sa-token-redis-template</artifactId>
                <version>${sa-token.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>${lombok.version}</version>
                            </path>
                        </annotationProcessorPaths>
                        <compilerArgs>
                            <arg>-parameters</arg>
                        </compilerArgs>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
