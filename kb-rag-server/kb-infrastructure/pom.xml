<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.kbrag</groupId>
        <artifactId>kb-rag-server</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>kb-infrastructure</artifactId>
    <name>kb-infrastructure</name>
    <description>Adapters for Elasticsearch, Qdrant, MinIO, model providers and the parser service</description>

    <dependencies>
        <dependency>
            <groupId>io.kbrag</groupId>
            <artifactId>kb-domain</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>co.elastic.clients</groupId>
            <artifactId>elasticsearch-java</artifactId>
        </dependency>
        <!--
          Qdrant is reached over its REST API through the RestClient that spring-boot-starter-web above
          already provides, so the vector route adds no dependency of its own. The gRPC SDK would pull a
          protobuf and Netty lineage to keep aligned for a surface this port does not need.
        -->
        <dependency>
            <groupId>io.minio</groupId>
            <artifactId>minio</artifactId>
        </dependency>
        <!--
          Bolt driver of the knowledge graph, requirement section 4.9. Only loaded when NEO4J_URI is set:
          a deployment without a graph never creates a driver, so the jar is inert rather than optional.
        -->
        <dependency>
            <groupId>org.neo4j.driver</groupId>
            <artifactId>neo4j-java-driver</artifactId>
        </dependency>
        <!-- JWKS signature verification of OIDC id_tokens only. A pure library with no transitive
             dependencies, version pinned in the parent to the release Spring Security 6.3 uses; the
             full OAuth2 client stack would drag in a framework for one signed token check per login. -->
        <dependency>
            <groupId>com.nimbusds</groupId>
            <artifactId>nimbus-jose-jwt</artifactId>
        </dependency>
        <!-- Headless Chromium fetcher of M17 JS rendering. Version pinned in the parent; the Chromium
             binary is not bundled in the jar and is installed into the image at build time (see the
             Dockerfile), so a deployment without it simply fails a render_js source rather than the app. -->
        <dependency>
            <groupId>com.microsoft.playwright</groupId>
            <artifactId>playwright</artifactId>
        </dependency>
    </dependencies>
</project>
